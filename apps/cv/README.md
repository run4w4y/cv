# @cv/cv

Astro application for the public and private CV site.

The app owns the CV content contract, localized routes, rendering components,
private-content unlock flow, print layout, and social metadata. It consumes the
workspace content pipeline through `@cv/content-astro` and exposes small app
entrypoints for content, i18n, and URL helpers.
