# @cv/content-composer

Core composition utilities for turning repository modules into content manifests.

This package discovers content modules, normalizes registry keys, merges content
values, and exposes the contract types used by apps that own their own content
shape.
