# @cv/content-astro

Astro integration for the workspace content pipeline.

The package wires authored content into Astro through a Vite virtual module,
builds content artifacts during config setup, loads content snapshots for tools,
and provides the generated type surface consumed by the app.
