# @cv/content-build

Build-time artifact generation for authored content.

The package validates caller-supplied build data, creates deterministic content
manifests, emits public and private file artifacts, infers private profile input,
and mints private audience links for tooling. Consumers decide where config and
secrets come from.
