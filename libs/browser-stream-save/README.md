# @cv/browser-stream-save

Browser runtime helpers for saving byte data.

The package writes byte data as Blob-based downloads through `file-saver`. Its
Effect service API lets UI code save decrypted or generated files without owning
the browser download details.
