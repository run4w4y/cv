# @cv/private-content-link

Operator tool for minting private content audience links.

The tool reads the private content root key from the workspace environment,
derives the requested profile content key, and produces audience/profile URLs
for an explicit locale and profile slug. It does not load application source
content or the CV app content contract.
